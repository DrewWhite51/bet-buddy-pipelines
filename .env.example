# AWS Configuration
S3_BUCKET_NAME=your-s3-bucket-name
AWS_REGION=us-east-1

# Database Configuration (PostgreSQL)
DATABASE_CONNECTION_STRING=Host=localhost;Port=5432;Database=sportsbetting;Username=your_username;Password=your_password

# AWS credentials (if not using AWS CLI profiles or IAM roles)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
