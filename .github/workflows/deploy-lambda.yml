name: Deploy Lambda Functions

on:
  push:
    branches: [ main ]
    paths:
      - 'src/HistoricalLinesScraper/**'
  workflow_dispatch:  # Allows manual trigger from GitHub UI

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'
    
    - name: Install AWS Lambda Tools
      run: dotnet tool install -g Amazon.Lambda.Tools
    
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1  # TODO: Change to your AWS region
    
    # TODO: Uncomment when ready to actually deploy
    # - name: Deploy Historical Lines Scraper
    #   run: |
    #     cd src/HistoricalLinesScraper
    #     dotnet lambda deploy-function HistoricalLinesScraper \
    #       --function-role ${{ secrets.LAMBDA_EXECUTION_ROLE_ARN }}